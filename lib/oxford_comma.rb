def oxford_comma(array)
   if array.length == 2 
    puts array.join(" and ")
end

def oxford_comma(array)
  if array.length == 3
    puts array.join(", ", " and ")
  else array.length > 3 
    puts array.join << element
  end
end
